import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  // site: 'https://nmformica.com',
  site: "https://nmforms.github.io/",
  base: "brooklyncreativeco.com",
});
